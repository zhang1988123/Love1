<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.love">
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/zzz"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/zzz"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.Main2bannerActivity" />
        <activity android:name=".activity.Main3ShouActivity" />
        <activity android:name=".activity.LastActivity" />
        <activity android:name=".activity.Main4WebViewActivity"></activity>
    </application>

</manifest>